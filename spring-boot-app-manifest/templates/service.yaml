apiVersion: v1
kind: Service
metadata:
  name: {{ include "spring-boot-app-manifest.fullname" . }}
  labels:
    {{- include "spring-boot-app-manifest.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "spring-boot-app-manifest.selectorLabels" . | nindent 4 }}
